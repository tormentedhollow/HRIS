/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LbPipe } from './lb.pipe';

describe('LbPipe', () => {
  it('create an instance', () => {
    const pipe = new LbPipe();
    expect(pipe).toBeTruthy();
  });
});
